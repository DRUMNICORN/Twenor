// use std::collections::HashMap;

mod filename;
// mod mp3_metadata;
// use twenor_log::Filename;
// use twenor_log::Log;
// static LOG: Log = Log::new(Filename::Reader);

// pub fn basic(_track_path: &String) -> HashMap<String, String> {
//     // LOG.debug(&format!("Getting metadata for {}", track_path));

//     // let file = filename::read(&track_path);
//     // let file = mp3_metadata::get_metadata(&track_path);

//     let metadata = HashMap::new();
//     // metadata.extend(file);
//     // metadata.extend(file);
//     metadata
// }

// pub fn metadata_max(
//     track_path: &str,
//     metadata: &HashMap<String, String>,
// ) -> HashMap<String, String> {
//     let mut metadata = HashMap::new();
//     metadata
// }
