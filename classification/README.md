# wave-classification-server

A python server which extracts features from audio files and stores them in a database. a neural network is then trained and can classify new audio files with the learned data.
